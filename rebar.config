{pre_hooks,
  [{"(linux)", compile, "./c_src/build_betree"},
   {"(linux)", compile, "make -C c_src"}]}.

{post_hooks,
  [{"(linux)", clean, "rm -rf be-tree"},
   {"(linux)", clean, "make -C c_src clean"}]}.

