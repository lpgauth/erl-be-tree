#!/usr/bin/env bash
## -*- sh-basic-offset: 2 -*-
set -x

if [ ! -d "be-tree" ]; then
    git clone http://github.com/FrankBro/be-tree
    cd be-tree
    make
    export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:"$PWD/build" 
fi

