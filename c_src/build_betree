#!/usr/bin/env bash
## -*- sh-basic-offset: 2 -*-
set -x

if [ ! -d "be-tree" ]; then
    git clone -b 'dirty' --single-branch --depth 1 http://github.com/lpgauth/be-tree
    cd be-tree
    make NIF=true
fi
