#!/usr/bin/env bash
## -*- sh-basic-offset: 2 -*-
set -x

if [ ! -d "be-tree" ]; then
    git clone http://github.com/FrankBro/be-tree
    cd be-tree
    make NIF=true
    mkdir -p ../priv
    cp build/libbetree.so ../priv/libbetree.so
fi

